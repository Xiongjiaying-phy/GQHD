# GQHD1–4 发布包说明

这是以之前 GQHD.zip 和 Loss.zip 为依据整理的论文复现准备包，不是重新拟合。参数和保存结果保持不变；没有混入后来的参考模型重新优化或耦合置零实验。

## 可以直接做什么

安装 `requirements.txt` 后，在包的根目录运行：

```sh
python scripts/reproduce.py check
python scripts/reproduce.py nuclear
python scripts/reproduce.py plot
python scripts/validate.py
```

分别检查保存结果、重新计算四组饱和性质、重绘模型曲线、运行验证。结果写入 `generated/`，不会覆盖原始图表。论文原图在 `figures/`，包括固定参考模型和观测色带；新绘图入口只重绘四组模型曲线，不伪造二维后验区域。

## 完整优化还需要什么

需要补齐两份外部数据：原始 BPS 表和 GW170817 低自旋 PhenomPNRT 后验样本。准确路径及 SHA-256 在 `data/external/Required.json`。BPS 表来源/授权尚未完成核实，原始观测样本也未直接放入公开包。不能任意替换文件后仍声称精确复现。

补齐后运行 `python scripts/check_external.py`，再按英文 README 运行潮汐微调。起点是保存的 HIC 微调结果；最终参数在 `models/`。本次没有重跑全部优化及 GW 积分。

## 已检查的内容

- 四组保存的 loss 分项之和与总值一致。
- 四组饱和性质从参数重新计算，与保存结果一致。
- 使用本机历史 BPS 表验证依赖导入；复算 HIC、核物质惩罚和保存 MR 曲线上的 profile 分数通过。
- MR 与核物质图的绘图入口运行通过。
- 检查 Python 语法及常见私人路径/敏感信息标记；没有上传或新建远程仓库。

测试证据见 `audit/Validation.json`。依赖版本为本次测试环境，而非声称原优化环境的精确锁定。

## 上传前必须确认

1. 代码作者、版权归属及许可证，当前未擅自指定 MIT/GPL，也未虚构作者或 DOI。
2. BPS、HIC 数字化边界与图片中观测区域的来源和再分发权限。
3. 论文描述与 `docs/Methods.md` 的实现一致：原始锚点不等于局部起点；软惩罚不等于硬筛选；稳定性只限检查网格。
4. 历史 MR 数组有极低质量、半径触及 100 km 数值保护上限的点；它们不应作为已验证的物理解。数据保留以便追溯，绘图按原论文半径窗口显示，问题未被包装工作修复。

完整说明见英文 README 和 docs。最新 PDF 未提供，本包按实际保存代码核对，不能称为逐页审过最终 PDF。
